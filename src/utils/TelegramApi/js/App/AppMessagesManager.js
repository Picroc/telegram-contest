export default class AppMessagesManagerModule {
	messagesFrames = {};
	maxMessageIds = {};

	saveMessages = (messages = []) => {
		this.messagesFrames[peerId] = this.messagesFrames[peerId] || [];

		messages.forEach(message => )
	}
}